Initially, we have SpringBoot up and running. The webpage is we want to access is locked behind a login screen by Spring Boot, and a controller we call "AppSecurityConfig" is handling the login credentials. 

But how will we handle details like password constraints and the like? That will be for later modules.
